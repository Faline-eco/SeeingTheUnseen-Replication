# torch/torchvision come from the base image (2.6.0 + cu124) - do not reinstall,
# a pip resolve would pull a different CUDA build.
#
# Versions are pinned to D:\PipelineStep2\.venv-neural, the environment the
# renders and the integrator self-test (r=0.9996 vs the GL renderer) were
# produced in. Embeddings and geometry computed here must be comparable with
# what the workstation produced, so this is parity, not caution.
numpy==2.4.4
opencv-python-headless==5.0.0.93   # headless build of the workstation's cv2 5.0.0.93

# --- geometry: the GL-free integrator ---------------------------------------
# render_embedding_field.py replaces alfspy's OpenGL integral with embree
# ray-casts + torch grid_sample, so the container needs the ray caster but no
# GL. trimesh 4.x is required for embreex to be picked up (`has_embree`).
trimesh==4.6.6
embreex==2.17.7.post5
pyrr==0.10.3               # camera/projection maths in alfspy + build_virtual_camera
scipy==1.17.1

# Present only to satisfy an import, NOT to render with. alfspy's
# core.convert imports core.rendering (for type annotations), which imports
# moderngl at module scope — so even the GL-free geometry path fails to import
# without it. `import moderngl` touches no driver; only create_context() would,
# and nothing here calls it. Rendering still belongs on the workstation.
moderngl==5.12.0
glcontext==3.0.0

# The rest of alfspy's declared dependencies. alfspy re-exports eagerly
# (core.convert -> core.rendering -> render -> util.gltf -> ...), so importing
# even a pure-geometry helper drags in the whole tree. Installing them is
# cheaper than maintaining a fork with lazy imports.
gltflib==1.0.13
Pillow==12.2.0
Rtree==1.3.0               # trimesh[easy] spatial index
matplotlib==3.11.1
Office365-REST-Python-Client==2.6.2   # alfspy's DEM download helper (unused
                                      # here: allow_dem_download is false)
# Pinned, not >=4.56: DINOv3 preprocessing must be byte-identical to the
# workstation's, or embeddings computed on the two machines are not comparable.
# This is the version the local runs used.
transformers==5.14.1
safetensors==0.8.0
scikit-learn==1.9.0        # PCA reduction of the 1280-dim embeddings
ultralytics==8.4.87        # metrics only (ap_per_class/box_iou); matches the
                           # version the released detectors were trained with
pyyaml>=6.0
tqdm>=4.66
